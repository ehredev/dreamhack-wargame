if((int)$input_id == $id && strlen($input_id) === 10){

php 형변환에 대해 알아야되는 문제
php8.0 이전의 버전에서는 문자열을 int로 변환시 맨 앞에 숫자가 오지 않는 경우 0이 된다. 또한 int == "문자열"은 자동으로 문자열의 형 변환을 시도하고, 그러므로 0이 될 확률이 높다
이를 이용해서 문제를 풀면 된다.
